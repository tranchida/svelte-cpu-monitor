[{'svelte': 'import { \n    Chart', "chart.js';\n\n  Chart.register(LineController, LineElement, PointElement, LinearScale, Title, Tooltip, Legend);\n\n  let chart;\n  let canvasEl;\n  let cpuData = [];\n\n  async function fetchCpuUsage() {\n    try {\n      const res = await fetch('/api/cpu": 'const data = await res.json();\n      return data.cpu;'}, {'CPU': ", err);\n      return 0;\n    }\n  }\n\n  onMount(async () => {\n    const ctx = canvasEl.getContext('2d');\n    chart = new Chart(ctx, {\n      type: 'line", 'data': {'labels': [], 'datasets': [{'label': 'Utilisation CPU (%)', 'data': [], 'borderColor': '#4CAF50', 'backgroundColor': 'rgba(76, 175, 80, 0.2)', 'fill': True}]}, 'options': {'responsive': True, 'scales': {'y': {'beginAtZero': True, 'max': 100}}, 'animation': {'duration': 0}}}, {'x': 'now', 'y': 'cpu'}, [0], {'padding': 2, 'font-family': 'sans-serif;'}, {'max-width': 100, 'margin-top': 1}]